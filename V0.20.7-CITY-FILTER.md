# v0.20.7 City filter

Adds a shared Cities dropdown to Trecento and Dutch/Flemish views. Select all is the default. Individual place checkboxes hide/show artists and incident edges, update the count badge and artist drawer, and refit the visible graph. Filters are maintained separately for each network. No database schema or SQL changes are required.
