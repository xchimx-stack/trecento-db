import fs from "node:fs";
const index=fs.readFileSync("public/index.html","utf8");
const discover=fs.readFileSync("public/discover.html","utf8");
const need=(s,x,l)=>{if(!s.includes(x))throw new Error(`${l}: missing ${x}`)};
const forbid=(s,x,l)=>{if(s.includes(x))throw new Error(`${l}: forbidden ${x}`)};
need(index,'value="ULAN" checked',"ULAN default");
forbid(index,'value="Wikipedia" checked',"Wikipedia default");
need(index,'id="relationshipKeyBtn"',"mobile relationship key");
need(discover,'id="completion"',"discovery completion");
need(discover,'id="admitReady"',"admission");
console.log("PASS: retained v0.15 regression contract");
