import fs from "node:fs";
const index=fs.readFileSync("public/index.html","utf8");
const graph=fs.readFileSync("api/graph.js","utf8");
const need=(s,x,l)=>{if(!s.includes(x))throw new Error(`${l}: missing ${x}`)};
need(index,"scheduleRender","mobile render throttle");
need(index,"anchorViewToMajorNode","initial graph anchor");
need(index,"animatePanToArtist","drawer navigation animation");
need(index,"relationshipSortRank","relationship sorting");
need(graph,"viaf_id","VIAF graph payload");
console.log("PASS: retained v0.16 regression contract");
